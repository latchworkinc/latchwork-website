// Flip to false once new positions open up and applications should resume.
// Note: careers page keeps listing roles regardless — this only gates
// whether /apply and /interview accept submissions.
export const NO_OPEN_POSITIONS = true;
